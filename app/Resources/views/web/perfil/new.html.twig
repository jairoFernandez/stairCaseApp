{% extends 'web/dashboard.html.twig' %}

{% block body %}
    <h1>Crear mi Perfil</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Crear" class="btn btn-success" />
    {{ form_end(form) }}

    
            <a href="{{ path('admin_profile_index') }}">Volver</a>
   
{% endblock %}
